export interface createNote {
    id: string
    title: string
    content: string
}

export interface updateNote {
    id: string
    title: string
    content: string
}

export interface deleteNote {
    id: string
}
